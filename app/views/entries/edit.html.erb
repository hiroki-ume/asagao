<% @page_title = "ブログ記事の編集" %>
<h1><%= @page_title %></h1>

<div class="toolbar">
  <%= link_to "記事の表示に戻る", @entry %>
</div>

<%= form_for @entry do |f| %>
  <%= render "form", f: f %>
  <div>
    <%= f.submit %>
  </div>
<% end %>
