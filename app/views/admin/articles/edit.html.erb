<% @page_title = "ニュース記事の編集" %>
<h1><%= @page_title %></h1>

<div class="toolbar">
  <%= link_to "記事の詳細に戻る", [:admin, @article] %>
</div>

<%= form_for [:admin, @article] do |f| %>
  <%= render "form", f: f %>
  <div>
    <%= f.submit %>
  </div>
<% end %>
