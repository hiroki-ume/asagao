<% @page_title = "ブログ記事の画像の編集" %>
<h1><%= @page_title %></h1>
<h2><%= @entry.title %></h2>

<% url = entry_images_path(@entry, @image) %>
<%= form_for(@image, url: url) do |f| %>
  <%= render "form", f: f %>
  <%= f.submit %>
<% end %>
